# ===== Blockchain Configuration =====
# Ethereum RPC endpoint (local or remote)
RPC_URL=http://127.0.0.1:8545

# Deployed contract address
CONTRACT_ADDRESS=0x...

# ===== Private Keys (KEEP SECRET - Never commit!) =====
# Registry role private key (has permission to approve batches)
PRIVATE_KEY_REGISTRY=0x...

# Supplier role private key (for testing - use x-wallet-address header in production)
PRIVATE_KEY_SUPPLIER=0x...

# Airline role private key (for testing - use x-wallet-address header in production)
PRIVATE_KEY_AIRLINE=0x...

# ===== Wallet Addresses =====
# Public address of the supplier account
SUPPLIER_ADDRESS=0x...

# Public address of the airline account
AIRLINE_ADDRESS=0x...

# ===== MongoDB Configuration =====
MONGO_URI=mongodb://localhost:27017/saf

# ===== Server Configuration =====
PORT=5000
NODE_ENV=development

# ===== CORS Configuration =====
# Comma-separated list of allowed origins
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# ===== Optional Configuration =====
# Directory for mapping wallet addresses to airline names
# Format: {"0xwalletaddress": "Airline Name", ...}
AIRLINE_DIRECTORY={}

# Directory for mapping supplier addresses to private keys
# Format: {"0xwalletaddress": "0xprivatekey", ...}
SUPPLIER_PRIVATE_KEYS={}

# Default bid expiry time in hours
DEFAULT_BID_EXPIRY_HOURS=72

# JWT Secret for future authentication implementation
JWT_SECRET=your-secret-key-change-in-production
